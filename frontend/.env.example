# Set to "true" to use mock data (localStorage), "false" to use backend API
VITE_USE_MOCK_DATA=true

# Backend API endpoint (only used when VITE_USE_MOCK_DATA=false)
VITE_API_ENDPOINT=http://localhost:8000
